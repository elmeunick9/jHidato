    O- Guardar game (amb estat) -> a carpeta amb username.
    - Guardar ranking i actualitzar. (username substr(5), ordre per puntuació. Temps jugat com a param.)
    - Afegir password a carpeta user.

Capa Presentació:
    - Ctrl. User.
    - Ctrl. Game. IMPORTANT!
    - Ctrl. Ranking.

    R- Pantalla inici
        Dues subdivisions:
        Login amb selectpicker.
        Register amb:
        - Text box amb username.
        - Boto register.

    - Seleccionar Hidato:
        - Carregar partides en curs o finalitzats: carpeta user.
        - Seleccionar acció (File > Load) | (File > Load Template) | (Generate).

    - Si Generate:
        - Si custom: pantalla amb paràmetres.
        - Si no es genera segons la dif selec.

    - Carregar "widget" amb l'hidato.

-* Millorar solver i generator.

-Afegir event de click
-afegir botó de solver
-Afegir botó de desfer
-Afegir triangles
-Afegir Hexagons

-Al seleccionar Load mostrar explorador d'arxius?

-Al guardar com template:
 - Crear a game saveAsTemplate();
 - A CustomFile crear SaveAsTemplate() i que guardi a la carpeta templates.
 - Aplicar en el controlador de persistencia.



